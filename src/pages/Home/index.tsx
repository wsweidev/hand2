import type { NextPage } from "next";
import ListingsDisplay from "@src/components/ListingsDisplay";

const Home: NextPage = () => {
    return (
        <>
            <ListingsDisplay />
        </>
    );
};

export default Home;
